
{
  "name": "gemini-ai-companion",
  "version": "1.0.0",
  "description": "An interactive web application to create and chat with a customizable AI companion.",
  "private": true,
  "scripts": {
    "dev": "echo 'Run index.html in a live server' && exit 1",
    "build": "echo 'No build step required for frontend, Vercel will build API routes.'"
  },
  "dependencies": {
    "@google/genai": "^1.10.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vercel/node": "^3.2.3",
    "typescript": "^5.5.4"
  }
}
